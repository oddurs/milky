# The single source of truth for release identity. Sourced by bundle.sh and
# release.sh so a version bump happens in one place.
VERSION="0.1.0"
BUILD_NUMBER="1"
BUNDLE_ID="com.oddurs.milky"
MIN_MACOS="14.0"
