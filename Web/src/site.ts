// Where the project lives. Kept in one place so a rename does not mean hunting
// through components for hard-coded URLs.
export const repo = 'https://github.com/oddurs/milky';
export const releases = `${repo}/releases/latest`;
